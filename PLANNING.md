# 지은 · 재준 모바일 청첩장 기획서

| 항목      | 내용                                   |
| --------- | -------------------------------------- |
| 예식      | 2026-12-06(일) 14:00 KST               |
| 콘셉트    | ivory ticket × sky accent 보딩패스     |
| 스택      | React, TypeScript, CSS Modules, Motion |
| 문서 기준 | 2026-09-07 현재 코드                   |

**스펙 제외:** 방명록, RSVP, 카카오페이 송금  
**공개 전 재확인:** 연락처·계좌 등 개인정보

현재 코드를 구현 현황의 기준으로 삼습니다. 남은 작업은 **실제 정보 추가**와 **사진 모달** 두 가지입니다. 코드에 정보가 등록되어 있다는 사실과 해당 정보가 최종 확인되었다는 상태는 구분합니다.

---

## 1. 현재 구현

**홈 화면 순서:** `BannerSection` → `GreetingSection` → `TicketSection` → `GallerySection` → `ContactSection` → `FooterSection`

홈은 `src/domains/home`, 전체 갤러리는 `src/domains/gallery`에서 구성합니다. `App.tsx`의 라우터가 두 화면을 전환하며, `FooterSection`은 두 화면에 공통으로 표시됩니다.

| 영역           | 구현 내용                                                                            | 상태                             |
| -------------- | ------------------------------------------------------------------------------------ | -------------------------------- |
| Banner         | `JIEUN & JAEJUN`, 날짜·요일, 예식장명, 메인 사진, `두 사람의 새로운 여정`, 등장 모션 | 이미지·문구 반영                 |
| Greeting       | 제목 `초대합니다`, 양가 가족 소개, 초대 인사말                                       | 인사말 반영                      |
| Ticket         | 일시·홀, ICS 캘린더 추가, 주소 복사, 보딩패스 UI, 카운트다운, 네이버·카카오맵        | 구현됨                           |
| GallerySection | 사진 5장 미리보기, 스크롤 진입 모션, `전체 사진 보기` 링크                           | 구현됨                           |
| GalleryMain    | 별도 페이지에서 사진 30장 표시, 지연 로딩, 뒤로가기                                  | 목록 구현됨, 사진 모달 미구현    |
| Contact        | 신랑·신부·혼주 전화/문자, 계좌 모달·복사, 복사 성공 시 모달 닫기·토스트              | 기능 구현됨, 실제 정보 추가 필요 |
| Footer         | 제작자·저작권                                                                        | 구현됨                           |

**공용:** `SectionTitle`, `MapLink`, `Modal`, `Portal`, `Toast`·`ToastProvider`, 아이콘

**훅:** `useOpenKakaoExternalBrowser`, `useCountdown`, `useIntersectionObserver`, `useToast`, `useTimeout`

**기존 계좌 모달:** 닫기 버튼·배경·Escape·브라우저 뒤로가기, 포커스 관리, 배경 스크롤 잠금

**데이터 (`WEDDING_INFO`)**

- 코드 반영: 이름·관계, 예식 일시, 장소·주소·지도 URL
- 계좌 등록: 신랑·신부 본인, 신랑측 아버지·어머니, 신부측 어머니
- 계좌 미등록: 신부측 아버지. 현재 해당 계좌 버튼은 표시하지 않음
- 플레이스홀더: 신랑·신부·양가 부모님 연락처 6곳 모두 `010-0000-0000`
- 교통·주차·식사·권장 도착 정보는 현재 데이터에 없음. 실제로 전달·확정된 정보만 반영

배너 이미지·문구는 `BannerSection`, 인사말은 `GreetingSection/Messages`, 갤러리 사진은 각 갤러리의 `_images`와 `_constants`에서 관리합니다. 계좌번호·전화번호 원문은 이 문서에 중복 기재하지 않습니다.

---

## 2. 목표

- 첫 화면에서 누구의 결혼식인지·날짜를 바로 이해한다.
- 일시·장소·지도를 짧게 확인하고, 연락·계좌 복사를 모바일에서 쉽게 한다.
- 항공권 콘셉트를 유지하면서 정보 전달을 방해하지 않는다.

---

## 3. 디자인

> 두 사람이 함께 출발하는 새로운 여행의 보딩패스

- 배경·면: `--color-canvas` / `--color-paper` (아이보리)
- 강조: `--color-sky`, `--color-sky-deep`
- 폰트: display `Cormorant Garamond`(+ Noto Serif KR), body Pretendard 스택
- 모티프: 절취선, 경로선, 바코드·stub (장식)

| 항공권           | 청첩장 (코드 기준)          |
| ---------------- | --------------------------- |
| Route            | `JE/JIEUN` → `JJ/JAEJUN`    |
| Flight           | `WEDDING`                   |
| Gate / Terminal  | `8F` / `VISION`             |
| Stub             | `JJ-2026-1206`              |
| Passenger / Seat | `INVITED GUEST` / `WITH US` |

---

## 4. 화면 구조와 배포 경로

**홈:** 배너 → 인사·가족 → 일시·티켓·지도 → 갤러리 미리보기 → 연락·계좌 → 푸터

**전체 갤러리:** 뒤로가기 → `우리의 순간` → 사진 목록 → 푸터

| 화면        | 앱 내부 경로 | 이동 방식                                                                                  |
| ----------- | ------------ | ------------------------------------------------------------------------------------------ |
| 청첩장 홈   | `/`          | 청첩장 진입, 갤러리에서 복귀                                                               |
| 전체 갤러리 | `/gallery`   | 홈의 `전체 사진 보기` 링크. 뒤로가기는 앱 이동 기록이 있으면 이전 화면, 없으면 홈으로 이동 |

- 배포 주소: [지은 · 재준 모바일 청첩장](https://tk-moon.github.io/jieun-jaejun-wedding-2026-12-06/)
- Vite `base`: 개발 서버는 `/`, 프로덕션 빌드·미리보기는 `/jieun-jaejun-wedding-2026-12-06/`
- `BrowserRouter`의 `basename`은 `import.meta.env.BASE_URL`을 사용해 배포 하위 경로를 공유
- 빌드 시 `dist/gallery/index.html`도 생성해 갤러리 직접 접속·새로고침에 대응
- GitHub Pages 워크플로: 의존성 설치 → 포맷 검사 → lint → build → `dist/` 배포

계좌·전화는 동의받은 정보만 게시한다. 검색 제외는 `noindex`, AI 수집 억제는 도메인 루트의 `robots.txt`로 요청한다. 실제 색인 여부는 별도 확인하며 URL·공개 코드 접근은 막지 않는다.

---

## 5. 품질

- 모바일 우선, 최소 320px 대응을 기준으로 가로 스크롤 없이 검수
- 공통 프레임 `--layout-frame-width: min(100%, 560px)`, 좌우 여백 `--layout-inline-padding: 16px`
- 모션은 정보·상태 보조 범위, `prefers-reduced-motion` 존중
- 핵심 정보는 텍스트, 아이콘 버튼에 `aria-label`
- 공개 저장소·배포 시 계좌·연락처 노출을 전제로 공개 범위 결정
- 공통 품질 검사: `npm run check`로 포맷·lint·타입 검사·프로덕션 빌드 확인

---

## 6. 남은 작업

| 항목           | 작업 내용                                                                             | 완료 기준                                                                                       |
| -------------- | ------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------- |
| 실제 정보 추가 | 연락처 6곳을 실제 번호로 교체하고, 전달·확정된 예식·안내·계좌 정보의 추가·수정을 반영 | 게시 대상 정보 확인, 임시 연락처 교체, 표시 내용과 전화·문자·계좌 복사 동작 검수                |
| 사진 모달      | 사진 선택 시 해당 사진을 크게 볼 수 있는 모달 구현                                    | 선택한 사진 표시, 닫기 후 원래 화면 복귀, 키보드·모바일에서 열기·닫기와 포커스·스크롤 동작 검수 |

아직 전달되지 않은 실제 정보는 **추후 전달 예정**으로 두고 임의로 작성하지 않습니다. 미등록 계좌는 게시 여부와 정보를 확인한 경우에만 추가합니다. 기존 기획의 공유 UI·OG 이미지·파비콘·추가 모션·통계 등은 별도 남은 작업으로 관리하지 않습니다.

**반영 후 검수:** 실제 정보는 실기기에서 지도·전화·문자·ICS·복사를 확인하고, 사진 모달은 열기·닫기·화면 복귀를 확인합니다. 코드 변경 시 `npm run check`를 실행합니다.

---

## 7. 체크리스트

| 항목                                 | 상태                                                                 |
| ------------------------------------ | -------------------------------------------------------------------- |
| 이름·부모님 관계·예식 일시·장소      | 코드 반영, 실제 정보 최종 확인 필요                                  |
| 계좌                                 | 신랑·신부 및 혼주 3명 등록, 게시 정보 최종 확인 필요                 |
| 연락처·추가 실제 정보                | **남은 작업**. 연락처 6곳은 임시 번호, 추가 정보는 전달·확정 후 반영 |
| 초대 인사말·배너 이미지·문구         | 코드 반영                                                            |
| 갤러리 미리보기·전체 사진 페이지     | 구현됨. 미리보기 5장, 전체 사진 30장                                 |
| 사진 모달                            | **남은 작업**. 현재 사진 목록에는 모달 열기 동작 없음                |
| 배포 하위 경로·갤러리 직접 접속 대응 | 코드·빌드 설정 반영                                                  |
| 검색 노출                            | 검색 제외 설정 적용, 실제 색인 여부 미확인                           |
| RSVP·방명록·카카오페이               | 스펙 제외                                                            |
